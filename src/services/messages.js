// src/services/messages.js
// Reads and writes message history.
// This will eventually manage the conversation log tab in Google Sheets.

export async function saveMessage() {
  // Placeholder for later.
  return true;
}

export async function getThreadHistory() {
  // Placeholder for later.
  return [];
}
